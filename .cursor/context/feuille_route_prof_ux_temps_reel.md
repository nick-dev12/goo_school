# Feuille de route UX professeur — navigation sans reload & temps réel

**Statut : R0–R5 livrés.** Détail : store `docs/feuille-route-prof-ux-temps-reel.md` (sections synthèse + vagues).

Branches : `cursor/prof-ux-r0-r1-a40c`, `r2`, `r3`, `r4`, `r5` (`-a40c`).

Tests : `school_admin.tests.test_prof_hub_nav_live`, `school_admin.tests.test_prof_nav_trail` (`--keepdb`).
