/**
 * Ajouter Professeur - JavaScript
 * Fonctionnalités pour la page d'ajout de professeur
 */

document.addEventListener('DOMContentLoaded', function() {
    console.log('Page d\'ajout de professeur chargée');
    
    // Aucune validation côté client - tout est géré par Django
    console.log('Formulaire initialisé - validation côté serveur uniquement');
});
